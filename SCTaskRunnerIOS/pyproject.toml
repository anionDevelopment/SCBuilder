[build-system]
requires = ["setuptools >= 82.0.1"]
build-backend = "setuptools.build_meta"

[project]
name = "sctaskrunnerios"
version = "1.2.10"
description = "SCTaskRunnerIOS provides an HTTP-server that builds iOS-app build-steps (e.g. flutter ios-builds) natively on a macOS-host on behalf of remote clients."
readme = { file = "ReadMe.md", content-type = "text/markdown" }
requires-python = ">=3.11"
authors = [
    { name = "Marius Göcke", email = "marius.goecke@gmail.com" },
]
classifiers = [
    "Intended Audience :: Developers",
    "License :: OSI Approved :: GNU General Public License v3 (GPLv3)",
    "Operating System :: MacOS :: MacOS X",
]
dependencies = [
    "scriptcollection>=4.3.3",
]

[project.scripts]
sctaskrunnerios = "SCTaskRunnerIOS.SCTaskRunnerIOSCore:main"

[tool.setuptools]
zip-safe = false
include-package-data = true

[tool.setuptools.packages.find]
where = ["."]
include = ["SCTaskRunnerIOS*"]
exclude = ["SCTaskRunnerIOSTests*"]
